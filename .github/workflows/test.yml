name: Simple Test Workflow

on:
  push:
    branches:
      - main
      - feat/**
  workflow_dispatch:

jobs:
  test-job:
    if: ${{ vars.IS_PUBLIC_ENVIRONMENT == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print Hello World
        run: echo "Hello, World!"
